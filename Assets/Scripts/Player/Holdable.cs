using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Holdable : MonoBehaviour
{
    public Vector3 front = Vector3.forward; //normalized vector that tells held object how to rotate
}
